# Hong Kong Address Parser — Python dependencies
# Python 3.10–3.12 recommended (training notebooks were run on 3.12).
#
# This file is the DEFAULT (CPU) install. It is enough for *inference* and
# for reading / editing notebooks. It is NOT enough for practical training of
# XLM-RoBERTa-large — use requirements-gpu.txt on a CUDA machine.
#
# Install:
#   python -m venv .venv && source .venv/bin/activate
#   pip install -U pip
#   pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cpu

# --- Core (used by bert_parser.py, bilstm_parser.py, and both training notebooks)
torch==2.7.1+cpu
pytorch-crf==0.7.2
transformers==5.14.0
datasets==5.0.0
tqdm==4.68.4
numpy>=2.0.0,<3
sentencepiece>=0.2.0
protobuf>=5.0.0
safetensors>=0.5.0
huggingface_hub>=0.34.0
tokenizers>=0.21.0
filelock>=3.16.0
regex>=2024.0.0
pyyaml>=6.0.0
requests>=2.32.0
packaging>=24.0

# --- Jupyter (training is in .ipynb files)
ipykernel>=6.29.0
ipywidgets==8.1.5
jupyter>=1.1.0
notebook>=7.3.0

# --- Optional: dataset-preparation scripts (uncomment if you rebuild data)
# pandas>=2.2.0
# geojson>=3.1.0
# shapely>=2.0.0
# pypdf>=5.0.0
# pdfplumber>=0.11.0
